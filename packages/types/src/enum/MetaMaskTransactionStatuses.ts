export enum MetaMaskTransactionStatuses {
  UNAPPROVED = "unapproved",
  APPROVED = "approved",
  REJECTED = "rejected",
  SIGNED = "signed",
  SUBMITTED = "submitted",
  FAILED = "failed",
  DROPPED = "dropped",
  CONFIRMED = "confirmed",
}
